# Confluent Serverless Example

Creates Confluent Cloud Enterprise/serverless Kafka ingress PSC resources, a ClickPipes RPE, and optionally a Kafka ClickPipe.

The example does not create source records. Keep `create_clickpipe = false` until your topic exists and contains data to load.
